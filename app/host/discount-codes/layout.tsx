export const metadata = {
  title: "Discount codes",
  description: "Manage your discount codes",
};

interface DiscountCodesLayoutProps {
  children: React.ReactNode;
}

const DiscountCodesLayout = ({ children }: DiscountCodesLayoutProps) => {
  return children;
};

export default DiscountCodesLayout;
