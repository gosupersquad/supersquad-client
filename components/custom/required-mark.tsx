/** Shared required-field asterisk for forms. Use next to label text. */
const RequiredMark = () => (
  <span className="text-destructive" aria-hidden>
    *
  </span>
);

export default RequiredMark;
