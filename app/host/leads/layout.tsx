export const metadata = {
  title: "Leads",
  description: "Event leads, spots booked, and guest details",
};

export default function LeadsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
